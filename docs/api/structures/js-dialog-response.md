# JsDialogResponse Object

* `accept` boolean - Whether to accept (`confirm`/`prompt`) or dismiss the dialog. For `alert`, typically `true`.
* `promptText` string (optional) - Returned string when the dialog `type` is `prompt` and `accept` is `true`.
