# QuerySelectorDeepOptions Object

* `pierce` boolean (optional) - Search nested open/closed author shadow roots (default: `true`).
* `scrollIntoView` boolean (optional) - Scroll the matched element into view before measuring (default: `false`).
