# JsDialogDetails Object

* `type` string - Can be `alert`, `confirm`, or `prompt`.
* `message` string - Dialog message text from the page.
* `defaultPrompt` string (optional) - Default value for `prompt()`.
* `url` string (optional) - Frame URL when available.
* `isMainFrame` boolean (optional) - Whether the dialog came from the main frame.
